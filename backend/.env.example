# Copy this file to .env and fill in real values. .env is gitignored — never
# commit real secrets. This file documents what the backend needs to run.

# Powers the demo agent's live gpt-4o-mini calls (POST /api/ask). Without it,
# the agent endpoint returns a clear error instead of failing to boot.
OPENAI_API_KEY=your-key-here
